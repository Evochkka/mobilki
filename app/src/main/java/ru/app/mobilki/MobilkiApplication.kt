package ru.app.mobilki

import android.app.Application
// import ru.app.mobilki.data.AppDatabase // Раскомментируй, когда добавишь AppDatabase

class MobilkiApplication : Application() {
    // val database: AppDatabase by lazy { AppDatabase.getDatabase(this) } // Раскомментируй, когда добавишь AppDatabase
} 